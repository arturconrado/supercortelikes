import { Logo } from '@/components/logo';

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <main className="relative grid min-h-screen overflow-hidden bg-canvas lg:grid-cols-[1.05fr_.95fr]"><div className="grid-glow relative hidden border-r border-white/[.06] p-12 lg:flex lg:flex-col"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_35%,rgba(201,255,66,.12),transparent_32%)]"/><div className="relative"><Logo /></div><div className="relative my-auto max-w-xl"><div className="mb-6 inline-flex rounded-full border border-lime/20 bg-lime/[.06] px-3 py-1.5 text-xs font-semibold text-lime">IA feita para creators brasileiros</div><h1 className="text-balance text-5xl font-bold leading-[1.08] tracking-[-.04em] text-white">Um vídeo longo.<br/><span className="text-lime">Dezenas de histórias.</span></h1><p className="mt-6 max-w-lg text-base leading-7 text-zinc-500">Encontre os melhores momentos, crie legendas marcantes e publique cortes em qualquer formato — sem perder horas na timeline.</p><div className="mt-10 flex gap-8"><AuthMetric value="9:16" label="Reframe inteligente"/><AuthMetric value="AI" label="Viral score"/><AuthMetric value="1080p" label="Exportação"/></div></div><p className="relative text-xs text-zinc-700">© 2026 PicaShorts. Criado para a nova geração de creators.</p></div><div className="flex items-center justify-center px-5 py-10 sm:px-10"><div className="w-full max-w-md lg:hidden"><div className="mb-10"><Logo /></div></div><div className="w-full max-w-md">{children}</div></div></main>;
}
function AuthMetric({ value, label }: { value: string; label: string }) { return <div><p className="text-xl font-bold text-white">{value}</p><p className="mt-1 text-xs text-zinc-600">{label}</p></div>; }
